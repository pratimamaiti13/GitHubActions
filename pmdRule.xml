<?xml version="1.0"?>

<ruleset name="Apex Rules" xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <!-- Define rule priority (1: High, 2: Medium, 3: Low) -->

    <!-- Example rule: AvoidUsingHardCodedId -->
    <rule name="AvoidUsingHardCodedId" message="Avoid using hard-coded Ids in Apex code" class="ApexCodeUnit" externalInfoUrl="https://example.com/docs/rules/avoid-using-hard-coded-ids">
        <priority>1</priority>
        <description>
            Avoid using hard-coded Ids in Apex code. Use custom settings or custom metadata types to store Ids.
        </description>
        <properties>
            <!-- Add any rule-specific properties here -->
        </properties>
    </rule>

    <!-- Example rule: AvoidEmptyCatchBlock -->
    <rule name="AvoidEmptyCatchBlock" message="Avoid empty catch blocks in Apex code" class="ApexCodeUnit" externalInfoUrl="https://example.com/docs/rules/avoid-empty-catch-blocks">
        <priority>2</priority>
        <description>
            Avoid empty catch blocks in Apex code. Empty catch blocks can hide potential errors and make debugging difficult.
        </description>
        <properties>
            <!-- Add any rule-specific properties here -->
        </properties>
    </rule>

    <!-- Add more rules as needed -->

</ruleset>
